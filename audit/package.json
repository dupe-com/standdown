{
  "name": "@standdown/audit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Eval harness and F→A+ grading for browser-extension affiliate stand-down (standdown).",
  "scripts": {
    "fixtures:serve": "tsx fixtures/server.ts",
    "fixtures:selftest": "tsx fixtures/selftest.ts",
    "spike:mv3": "tsx spike/mv3-spike.ts"
  },
  "dependencies": {
    "standdown": "file:.."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.55.0",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0"
  }
}
